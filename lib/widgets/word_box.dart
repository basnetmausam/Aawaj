import 'package:flutter/material.dart';

class WordBox extends StatelessWidget {
  const WordBox({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
